{
	"name": "izanagi-react",
	"version": "0.0.1",
	"private": true,
	"engines": {
		"node": "6.9.0",
		"npm": "4.1.1"
	},
	"scripts": {
		"postinstall": "cd client && yarn install",
		"lint": "cd client && yarn run lint --silent",
		"build:test": "cd client && yarn run build:test --silent",
		"build:production": "cd client && yarn run build:production --silent",
		"build:development": "cd client && yarn run build:development --silent"
	},
	"cacheDirectories": ["node_modules", "client/node_modules"]
}
